<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ChessPoints" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Branded launch experience: ChessPoints king on the deep board green. -->
    <style name="Theme.ChessPoints.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_green_deep</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ChessPoints</item>
    </style>
</resources>
